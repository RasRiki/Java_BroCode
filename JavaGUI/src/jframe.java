public class jframe {
    public static void main(String[] args){

        /*JFrame frame = new JFrame(); // creates a frame
        frame.setTitle("JFrame Title");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // exit out application ( di solito al posto di exit c'è HIDE_ON_CLOSE)
        frame.setResizable(false); // prevent frame from being resize
        frame.setSize(420, 420); // Sets the x-dimension and y-dimension
        frame.setVisible(true);// make frame visible

        //ImageIcon image = new ImageIcon("Path files"); // Create an image icon
        // frame.setIconImage(image.getImage()); CHANGE ICON OF THE FRAME
        frame.getContentPane().setBackground(new Color(0x0000)); // change color of the background*/

        MyFrame myFrame = new MyFrame();


    }
}
